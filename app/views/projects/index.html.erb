</div>

    <div class="container">

      <div class="row">

        <div class="col-lg-12">
          <h1 class="page-header">Project Listing<small> A sample of my works.</small></h1>
        </div>

      </div>

      <% @projects.each do |project| %>

      <div class="row">

        <div class="col-lg-7 col-md-7">
          <%= link_to(image_tag("https://s3.amazonaws.com/portfolio.site.bucket/littlescreen.png"), "http://www.reddit.com") %>
        </div>

        <div class="col-lg-5 col-md-5", id="<%= dom_id(project) %>">
          <h3><%= project.name %></h3>
          <p><%= project.technologies_used %></p>
          <br/>
          <%= link_to 'View Project', project, :class => "btn btn-success" %>

          <% if policy(project).update? %>
          <%= link_to 'Edit Project', edit_project_path(project), :class => "btn btn-info" %>
          <% end %>

          <% if policy(project).destroy? %>
          <%= link_to 'Destroy Project', project, confirm: 'Are you sure?', method: :delete, :class => "btn btn-danger" %>
          <% end %>

          <% if policy(project).publish? && project.published? == false %>
          <%= button_to 'Publish', project_path(project, project: {published: true}), method: :put, :class => "btn btn-success", :id => "publishbutton" %>
          <% end %>
        </div>

      </div>

      <br/><br/>

      <% end %>

          <% if policy(Project).create? %>
          <%= link_to 'New Project', new_project_path, :class => "btn btn-success" %>
          <% end %>

    </div>
